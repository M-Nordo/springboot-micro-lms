server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/enrollmentdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

rabbitmq:
  host: localhost
  port: 5672